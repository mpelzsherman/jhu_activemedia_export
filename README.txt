This code will export files from the JHU ActiveMedia repository into a collection-based structure suitable for importing into a new DAM system.

Asset metatadata will also be exported in .csv format alongside each file.

Michael Pelz-Sherman
July 26, 2012
